## What this changes

<!-- One or two sentences. What is different after this merges? -->

## Why

<!-- The reason, not a restatement of the diff. Link an issue if there is one. -->

## How it was verified

<!-- What you actually ran, and what it said. "Tests pass" is weaker than
     "dotnet test: 28 passed" or "docker compose up --build, POST returned 201". -->

- [ ] `dotnet test` passes locally
- [ ] `docker compose up --build` brings up both services and the API responds
- [ ] CI is green on this branch

## Notes for the reviewer

<!-- Anything surprising: a decision that could reasonably have gone the other way,
     a piece worth looking at first, something deliberately left out of scope. -->
